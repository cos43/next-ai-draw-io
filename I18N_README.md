# 🌍 多语言支持快速开始

## 概览

FlowPilot 现已支持完整的中英文双语切换！所有翻译配置已准备就绪，共 **455+ 翻译项**。

## 快速使用

### 1. 切换语言

点击导航栏右侧的语言切换按钮：
- **中文** ⇄ **EN**

语言偏好会自动保存，下次访问时自动恢复。

### 2. 在代码中使用

```tsx
import { useLocale } from "@/contexts/locale-context";

function MyComponent() {
  const { t } = useLocale();
  
  return <button>{t("common.save")}</button>;
}
```

### 3. 带参数的翻译

```tsx
const count = 5;
<p>{t("history.totalVersions", { count })}</p>
// 输出: "共 5 个版本" (中文) 或 "Total 5 versions" (英文)
```

## 文件结构

```
locales/
  └── translations.ts              # 集中的翻译配置 (455+ 翻译项)

contexts/
  └── locale-context.tsx           # 语言管理 Context

components/
  └── language-switcher.tsx        # 语言切换按钮

docs/
  ├── i18n-guide.md               # 详细使用指南
  ├── i18n-migration-guide.md     # 组件迁移指南
  └── MULTILINGUAL_COMPLETE_SUMMARY.md  # 完整实现总结
```

## 翻译覆盖

已准备好以下模块的翻译（共 455+ 项）：

- ✅ **通用** (common) - 按钮、操作、状态等
- ✅ **导航** (nav) - 导航栏文本
- ✅ **工作区** (workspace) - 工作区相关
- ✅ **编辑器** (drawio) - DrawIO 编辑器
- ✅ **聊天** (chat) - 聊天界面
- ✅ **模型** (model) - 模型配置
- ✅ **历史** (history) - 版本历史
- ✅ **快捷操作** (quickAction) - 快捷按钮
- ✅ **简报** (brief) - FlowPilot Brief
- ✅ **比对** (comparison) - 模型比对
- ✅ **工具栏** (toolbar) - 智能工具栏
- ✅ **PPT** (ppt) - PPT Studio
- ✅ **错误/成功** (error/success) - 提示信息
- ✅ 以及更多...

## 组件适配状态

### ✅ 已完成
- 主页面 (`app/page.tsx`)
- 导航栏 (`components/workspace-nav.tsx`)
- Layout (`app/layout.tsx`)

### 📝 待适配
其他组件的翻译配置已准备就绪，只需简单的文本替换即可。

详细的组件列表和迁移步骤请参考：
- 📖 [`docs/i18n-migration-guide.md`](docs/i18n-migration-guide.md)

## 添加新的翻译

在 `locales/translations.ts` 中添加：

```typescript
export const zhTranslations = {
  myModule: {
    newKey: "新的中文文本",
  },
};

export const enTranslations = {
  myModule: {
    newKey: "New English text",
  },
};
```

然后在组件中使用：

```tsx
<div>{t("myModule.newKey")}</div>
```

## 特性

- ✅ **零依赖** - 无需额外的 npm 包
- ✅ **轻量级** - 约 8KB (gzip 压缩后)
- ✅ **自动检测** - 首次访问时自动检测浏览器语言
- ✅ **持久化** - 语言偏好自动保存
- ✅ **类型安全** - 完整的 TypeScript 支持
- ✅ **参数支持** - 支持动态参数如 `{count}`

## 文档

- 📖 [使用指南](docs/i18n-guide.md) - 详细的 API 和最佳实践
- 📖 [迁移指南](docs/i18n-migration-guide.md) - 组件迁移步骤
- 📖 [完整总结](MULTILINGUAL_COMPLETE_SUMMARY.md) - 实现细节和统计

## 示例

### 简单文本

```tsx
// ❌ 之前
<h1>智能助手</h1>

// ✅ 之后
<h1>{t("chat.title")}</h1>
```

### 带参数

```tsx
// ❌ 之前
<div>共 {count} 个版本</div>

// ✅ 之后
<div>{t("history.totalVersions", { count })}</div>
```

### 选项列表

```tsx
// ✅ 推荐
const options = [
  { value: "flowchart", label: t("diagramType.flowchart") },
  { value: "mindmap", el: t("diagramType.mindmap") },
];
```

## 测试

```bash
# 构建测试
npm run build

# 开发模式
npm run dev
```

访问 http://localhost:6002，点击语言切换按钮测试功能。

## 支持的语言

- 🇨🇳 中文 (zh)
- 🇬🇧 English (en)

## 扩展到更多语言

参考 [`docs/i18n-guide.md`](docs/i18n-guide.md) 中的"扩展支持更多语言"章节。

---

**提示**: 所有翻译配置已准备就绪！你可以立即在组件中使用 `t()` 函数，无需额外配置。
